overwrite: true
schema: "http://127.0.0.1/wordpress/graphql"
documents: null
generates:
  framework/graphql.ts:
    plugins:
      - "typescript"
      - "typescript-resolvers"
  framework/graphql.schema.json:
    plugins:
      - "introspection"
